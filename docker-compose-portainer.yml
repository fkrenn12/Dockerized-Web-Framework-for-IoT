services:
  portainer:
    image: portainer/portainer-ce:2.21.2
    command: -H unix:///var/run/docker.sock
    container_name: portainer
    restart: always
    #ports:
    # - ${PORT_PORTAINER}:${PORT_PORTAINER}
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "./.portainer_data:/data"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.rule=Host(`${SUBDOMAIN_PORTAINER}${DOMAIN}`)"
      - "traefik.http.routers.frontend.entrypoints=websecure"
      - "traefik.http.services.frontend.loadbalancer.server.port=${PORT_PORTAINER}"
      - "traefik.http.routers.frontend.service=frontend"
      - "traefik.http.routers.frontend.tls.certresolver=letsencrypt"
    networks:
      - backnet